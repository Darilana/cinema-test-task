{
    "name": "cinema-test-task",
    "version": "1.0.0",
    "repository": "https://github.com/Darilana/cinema-test-task",
    "license": "MIT",
    "devDependencies": {
        "bower": "^1.7.7",
        "http-server": "^0.9.0"
    },
    "scripts": {
        "postinstall": "bower install",
        "update-deps": "npm update",
        "postupdate-deps": "bower update",
        "prestart": "npm install",
        "start": "http-server -a localhost -p 8000 -c-1 ./app"
    }
}
